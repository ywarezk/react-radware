
/**
 * 
 * @param {String[]} strings 
 * ['hello ', 'foo ']
 * 0.345345
 * 42
 */
function hello(strings, arg1, arg2) {
	
}

hello(['hello', 'foo', 'bar']);

hello`
	hello ${Math.random()}
	foo ${10 + 32}
`